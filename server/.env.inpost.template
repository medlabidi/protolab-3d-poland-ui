# InPost ShipX API - Environment Configuration Template

# ============================================
# INPOST SHIPX API CREDENTIALS
# ============================================
# Obtain these from https://manager.paczkomaty.pl/
# After registering your business and completing company data

# Organization ID (found in "Ustawienia organizacji")
INPOST_ORGANIZATION_ID=

# API Token (click "Pokaż token" in API ShipX section)
INPOST_API_TOKEN=

# Geowidget Token (for frontend locker map widget)
INPOST_GEOWIDGET_TOKEN=

# ============================================
# ENVIRONMENT SETTINGS
# ============================================

# Sandbox mode for testing (true = sandbox, false = production)
INPOST_SANDBOX_MODE=true

# API Base URLs (automatically selected based on sandbox mode)
INPOST_API_BASE_URL=https://api-shipx-pl.easypack24.net
INPOST_SANDBOX_API_URL=https://sandbox-api-shipx-pl.easypack24.net

# ============================================
# WEBHOOK CONFIGURATION
# ============================================

# Webhook secret for verifying InPost callbacks
INPOST_WEBHOOK_SECRET=your_secure_random_string_here

# Your webhook URL (must be HTTPS)
# Example: https://api.protolab3d.pl/api/inpost/webhook
INPOST_WEBHOOK_URL=

# ============================================
# BUSINESS INFORMATION
# ============================================
# This information will be used for shipment sender details

# Company name
COMPANY_NAME=ProtoLab 3D Poland

# Sender address (for courier shipments)
SENDER_NAME=ProtoLab 3D
SENDER_EMAIL=orders@protolab3d.pl
SENDER_PHONE=+48123456789
SENDER_STREET=ul. Example Street
SENDER_BUILDING=10
SENDER_CITY=Kraków
SENDER_POST_CODE=30-000
SENDER_COUNTRY=PL

# ============================================
# SETUP INSTRUCTIONS
# ============================================
#
# 1. Register at https://manager.paczkomaty.pl/
# 2. Complete ALL company data (address + billing info)
# 3. Navigate to "Moje Konto" → "API ShipX"
# 4. Click "Generuj" to generate API Token
# 5. Copy Organization ID from "Ustawienia organizacji"
# 6. Copy API Token (click "Pokaż token")
# 7. Generate Geowidget token if needed
# 8. Add your domain to Geowidget allowed domains
# 9. Fill in the values above
# 10. Remove .template from filename (rename to .env)
#
# ============================================
# TESTING
# ============================================
#
# Start with INPOST_SANDBOX_MODE=true for testing
# InPost provides sandbox environment for development
# Switch to false when ready for production
#
# Test endpoints:
# - GET /api/inpost/points?city=Krakow
# - GET /api/inpost/points/KRA01M
# - POST /api/inpost/shipments (with test data)
#
